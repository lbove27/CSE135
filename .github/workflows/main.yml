name: scp files
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: copy file via ssh password
      uses: appleboy/scp-action@v0.1.4
      with:
        host: 167.71.18.104
        username: leland
        password: CSE135leland
        port: 22
        source: "./README.txt"
        target: ./var
        
        


